[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "kicaddy"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["kicad-sym"]

[project.scripts]
kicaddy = "main:main"
